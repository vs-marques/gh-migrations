-- PKG_DSV_V1_00001_BOOTSTRAP — placeholder
-- Substituir pelo DDL real do GhostWritter (projects, sparks, mural_nodes, mural_edges, …)

-- Exemplo (comentado):
-- CREATE SCHEMA IF NOT EXISTS ghostwritter;
-- CREATE TABLE IF NOT EXISTS ghostwritter.projects (
--   id UUID PRIMARY KEY,
--   name TEXT NOT NULL,
--   created_at TIMESTAMPTZ NOT NULL DEFAULT now()
-- );

SELECT 1; -- no-op até o primeiro pacote real
