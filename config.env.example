# Configurações de Banco — GhostWritter Migrations

Copie para `.env` e ajuste. Nunca commitar `.env` com secrets reais.

# =============================================================================
# DESENVOLVIMENTO
# =============================================================================
DB_DEV_HOST=localhost
DB_DEV_PORT=5436
DB_DEV_DATABASE=ghostwritter
DB_DEV_USER=admin
DB_DEV_PASSWORD=change_me

# =============================================================================
# HOMOLOGAÇÃO
# =============================================================================
DB_HMG_HOST=localhost
DB_HMG_PORT=5432
DB_HMG_DATABASE=ghostwritter_hmg
DB_HMG_USER=admin
DB_HMG_PASSWORD=change_me

# =============================================================================
# PRODUÇÃO
# =============================================================================
DB_PRD_HOST=localhost
DB_PRD_PORT=5432
DB_PRD_DATABASE=ghostwritter_prd
DB_PRD_USER=admin
DB_PRD_PASSWORD=change_me

# =============================================================================
# GERAL
# =============================================================================
DB_TIMEOUT=30
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
